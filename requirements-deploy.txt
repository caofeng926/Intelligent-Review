# Operational dependencies (sync scripts + prod WSGI server)
# Audit 2026-09-03 M4: 与 requirements.txt 配套使用
#
# 安装:
#   pip install -r requirements.txt        # 运行时
#   pip install -r requirements-deploy.txt # 部署侧
#   pip install -r requirements-dev.txt    # 开发工具链

paramiko>=3.4      # SSH/SFTP for CVM sync scripts (audit C3: 改用 RejectPolicy + load_host_keys)
gunicorn>=22.0     # Production WSGI server
